name: "Crystal CI"

on:
  pull_request:
    branches: 
      - main
  push:
    branches:
      - main

jobs:
  build_test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: shards install
      - name: Run tests
        run: crystal spec
